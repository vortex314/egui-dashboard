export component HelloWorld inherits Window {
    width: 400px;
    height: 400px;

    Text {
       y: parent.width / 5;
       x: parent.x + 150px;
       text: "Hello, world";
       color: green;
    }
}